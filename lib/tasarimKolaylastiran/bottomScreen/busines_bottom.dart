import 'package:flutter/material.dart';

class BusinesBottom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Business"),
    );
  }
}
